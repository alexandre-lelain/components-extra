---
name: Newsletter
menu: Components
route: /components/newsletter
---

import { Playground } from 'docz'
import Newsletter from 'components/Newsletter'
import {
  Extended,
  ExtendedNewsletter,
  ExtendedButton,
  ExtendedInput,
  ExtendedInputStory,
  Props,
} from '../docs-utils'

# Newsletter

<br/>

- [Newsletter](#)
  - [Newsletter.Input](#newsletterinput)
  - [Newsletter.Button](#newsletterbutton)

## Default

<Playground>
  {() => {
    const [email, setEmail] = React.useState('')
    return (
      <Newsletter
        title='Subscribe to our newsletter'
        description='Get exclusive offers every week!'
        caption="By subscribing, you agree to receive emails from us. Don't worry, we are not spamers :)"
      >
        <Newsletter.Input id="email" placeholder="Email..." value={email} onChange={setEmail} />
        <Newsletter.Button onClick={() => console.log('Thanks for subscribing!')}>
          Subscribe
        </Newsletter.Button>
      </Newsletter>
    )
  }}
</Playground>

## No caption

<Playground>
  {() => {
    const [email, setEmail] = React.useState('')
    return (
      <Newsletter
        title='Subscribe to our newsletter'
        description='Get exclusive offers every week!'
      >
        <Newsletter.Input id="email" placeholder="Email..." value={email} onChange={setEmail} />
        <Newsletter.Button onClick={() => console.log('Thanks for subscribing!')}>
          Subscribe
        </Newsletter.Button>
      </Newsletter>
    )
  }}
</Playground>

## No description

<Playground>
  {() => {
    const [email, setEmail] = React.useState('')
    return (
      <Newsletter
        title='Subscribe to our newsletter'
        caption="By subscribing, you agree to receive emails from us. Don't worry, we are not spamers :)"
      >
        <Newsletter.Input id="email" placeholder="Email..." value={email} onChange={setEmail} />
        <Newsletter.Button onClick={() => console.log('Thanks for subscribing!')}>
          Subscribe
        </Newsletter.Button>
      </Newsletter>
    )
  }}
</Playground>

## Title only

<Playground>
  {() => {
    const [email, setEmail] = React.useState('')
    return (
      <Newsletter
        title='Subscribe to our newsletter'
      >
        <Newsletter.Input id="email" placeholder="Email..." value={email} onChange={setEmail} />
        <Newsletter.Button onClick={() => console.log('Thanks for subscribing!')}>
          Subscribe
        </Newsletter.Button>
      </Newsletter>
    )
  }}
</Playground>

## Custom controls

<Playground>
   {() => {
    const [email, setEmail] = React.useState('')
    return (
      <Newsletter
        title='Subscribe to our newsletter'
        description='Get exclusive offers every week!'
        caption="By subscribing, you agree to receive emails from us. Don't worry, we are not spamers :)"
      >
        <input id="email" placeholder="Email..." type="email" value={email} onChange={setEmail}/>
        <button onClick={() => console.log('Thanks for subscribing!')}>
          Subscribe
        </button>
      </Newsletter>
    )
  }}
</Playground>

## Extended

<Extended name="Newsletter">
  <ExtendedNewsletter />
</Extended>

## Props

<Props of={Newsletter}/>

<br/>
<hr/>

## Newsletter.Button

### Default

<Playground>
  <Newsletter.Button onClick={() => console.log('Thanks for subscribing!')}>
    Subscribe
  </Newsletter.Button>
</Playground>

### Various props

<Playground>
  <Newsletter.Button
    color="secondary"
    endIcon={<span>:)</span>}
    size="large"
    onClick={() => console.log('Thanks for subscribing!')}
  >
    Subscribe now
  </Newsletter.Button>
</Playground>

### Extended

<Extended name="Newsletter.Button">
  <ExtendedButton onClick={() => console.log('Thanks for subscribing!')}>
    Subscribe now
  </ExtendedButton>
</Extended>

### Props

This component extends `Material's UI Button` so you can use all its props as well. Check out its [Api](https://material-ui.com/api/button/).

<Props of={Newsletter.Button} />

<br />
<hr />

## Newsletter.Input

### Default

<Playground>
  {() => {
    const [email, setEmail] = React.useState('')
    return <Newsletter.Input id="email" placeholder="Email..." value={email} onChange={setEmail} />
  }}
</Playground>

### Various props

<Playground>
  {() => {
    const [email, setEmail] = React.useState('')
    return (
      <Newsletter.Input
        endAdornment={<span>:)</span>}
        id="email"
        color="secondary"
        placeholder="components-extra@gmail.com"
        value={email}
        onChange={setEmail}
        label="Email"
      />
    )
  }}
</Playground>

### Error state

<Playground>
  {() => {
    const [email, setEmail] = React.useState('')
    return (
      <Newsletter.Input
        error
        id="email"
        placeholder="components-extra@gmail.com"
        value={email}
        onChange={setEmail}
      />
    )
  }}
</Playground>

### Extended

<Extended name="Newsletter.Input">
  <ExtendedInputStory />
</Extended>

### Props

This component extends `Material's UI OutlinedInput` so you can use all its props as well. Check out its [Api](https://material-ui.com/api/outlined-input/).

<Props of={Newsletter.Input} />
