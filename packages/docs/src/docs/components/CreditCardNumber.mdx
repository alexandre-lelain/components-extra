---
name: CreditCardNumber
menu: Components
route: /components/credit-card-number
---

import { Playground } from 'docz'
import CreditCardNumber from 'components/CreditCardNumber'
import { Extended, Props, StyledCreditCardNumber } from '../docs-utils'

# CreditCardNumber

## Default

<Playground>
  <CreditCardNumber id="card-number" onChange={() => console.log('onChange')} />
</Playground>

## Controlled value validation

<Playground>
  {() => {
    const [value, setValue] = React.useState('')
    const [isError, setError] = React.useState(false)
    React.useEffect(() => {
      setError(!isValueValid(value))
    }, [setError, value])
    const isValueValid = value => {
      const regex = RegExp(/[\D]+/)
      return value === '' || !regex.test(value)
    }
    return <CreditCardNumber error={isError} id="card-number" onChange={setValue} />
  }}
</Playground>

## Disabled

<Playground>
  <CreditCardNumber id="card-number" onChange={() => console.log('onChange')} disabled />
</Playground>

## Extended

<Extended name="CreditCardNumber">
  <StyledCreditCardNumber id="card-number" onChange={() => console.log('onChange')} />
</Extended>

## Props

This component extends `Material's UI OutlinedInput` so you can use all its props as well. Check out its [Api](https://material-ui.com/api/outlined-input/).

<Props of={CreditCardNumber} />